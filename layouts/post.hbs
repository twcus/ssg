<!DOCTYPE html>
<html lang="en">
    <head>
        {{!-- head should be a partial or template --}}
        <meta charset="utf-8">
        {{!-- TODO title and description should come from the page's frontmatter --}}
        <title>{{page.title}}</title>
        <meta name="description" content={{page.description}}>
        <meta name="author" content="Thomas Custer">
    </head>
    <body>
        {{> nav activeLink="Projects"}}
        <header>
            <div>
                {{!-- TODO title and description should come from the page's frontmatter --}}
                <h1>{{page.title}}</h1>
                <h2>{{page.description}}</h2>
            </div>
        </header>
        <main>
            {{{content}}}
        </main>
        <footer>
            <p>Copyright Thomas Custer</p>
            <p>Created with SSG</p>
        </footer>
    </body>
</html>